# Cloud Database Connection String
# For local testing, replace this with your PostgreSQL credentials
# For production (Render/Neon/Supabase etc), copy their connection string here
DATABASE_URL=postgresql://postgres:password@localhost:5432/smartcdss
